# Handmade Jewelry Platform

This is an online platform for artisans to sell handmade jewelry.

## Features

* Product catalog
* Shopping cart
* Payment gateway
* User reviews

## Installation

1. Clone the repository
2. Run `npm install` to install dependencies
3. Create a .env file and add your MongoDB URI and port
4. Run `npm start` to start the server